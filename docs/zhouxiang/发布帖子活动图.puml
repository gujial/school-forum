@startuml
title 活动图：发布帖子
skinparam backgroundColor white

|用户|
start
if (已登录?) then (否)
    :跳转登录页面;
    stop
else (是)
    :进入发帖界面;
endif

repeat
    :输入帖子标题和正文;
    :上传图片/媒体 (可选);
    :选择所属板块;
    :点击发布;

    |系统|
    :检查内容是否为空;
    :进行敏感词过滤/审核;

    if (审核通过?) then (否)
        :提示包含敏感词或内容违规;
        |用户|
        :修改内容;
    else (是)
        |系统|
        :保存帖子到数据库;
        :更新板块帖子列表;
        :提示发布成功;
        :跳转至帖子详情页;
        stop
    endif
@enduml