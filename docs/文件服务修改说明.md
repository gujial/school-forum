# 文件服务修改说明

## 修改概述

将原本依赖外部图片服务器的文件上传和下载功能修改为仅使用Node.js服务器实现。

## 主要修改内容

### 1. 新增文件下载API接口

#### 头像文件下载
- **文件路径**: `server/api/files/avatar/[id].ts`
- **API端点**: `GET /api/files/avatar/{userId}`
- **功能**: 直接通过Node.js服务器提供头像文件下载服务

#### 媒体文件下载
- **文件路径**: `server/api/files/media/[tweetId]/[filename].ts`
- **API端点**: `GET /api/files/media/{tweetId}/{filename}`
- **功能**: 支持图片和视频文件的直接下载，自动识别文件类型并设置正确的Content-Type

### 2. 修改文件上传接口

#### 头像上传接口 (`server/api/avatar/upload/[id].post.ts`)
- 移除了对外部图片服务器的依赖
- 将存储的URL从 `${runtimeConfig.image_server}/avatars/${userId}/${newFilename}` 改为 `/api/files/avatar/${userId}`

#### 图片上传接口 (`server/api/media/upload/images/[id].post.ts`)
- 移除了对外部图片服务器的依赖
- 将存储的URL从 `${runtimeConfig.image_server}/media/${tweetId}/${filename}` 改为 `/api/files/media/${tweetId}/${filename}`

#### 视频上传接口 (`server/api/media/upload/video/[id].post.ts`)
- 移除了对外部图片服务器的依赖
- 将存储的URL从 `${runtimeConfig.image_server}/media/${tweetId}/${filename}` 改为 `/api/files/media/${tweetId}/${filename}`

### 3. 配置文件修改

#### nuxt.config.ts
- 移除了 `image_server` 配置项
- 不再需要外部图片服务器配置

### 4. 文档更新

#### README.md
- 更新了部署说明，移除了图片服务器相关配置
- 添加了新的文件服务说明

## 技术特点

### 文件服务特性
1. **直接文件服务**: 通过Node.js服务器直接提供文件下载，无需额外的HTTP服务器
2. **自动类型识别**: 根据文件扩展名自动设置正确的Content-Type
3. **缓存支持**: 设置了适当的缓存头，提高性能
4. **错误处理**: 完善的错误处理机制，包括文件不存在、权限错误等

### 支持的文件类型
- **图片**: jpg, jpeg, png, gif, webp
- **视频**: mp4, avi, mov, webm

### 文件存储结构
```
dynamic/
├── avatars/
│   └── {userId}/
│       └── {avatar_file}
└── media/
    └── {tweetId}/
        ├── {image_files}
        └── {video_files}
```

## 部署说明

### 开发环境
```bash
pnpm install
pnpm run dev
```

### 生产环境
```bash
pnpm install
pnpm run build
cp -r .data .output
cd .output
mkdir dynamic
node ./server/index.mjs
```

**注意**: 不再需要额外的图片服务器，所有文件服务都由Node.js服务器直接提供。

## 测试

运行测试脚本创建测试环境：
```bash
node scripts/test-file-service.js
```

测试API端点：
- `GET /api/files/avatar/test-user`
- `GET /api/files/media/test-tweet/test-image.jpg`
- `GET /api/files/media/test-tweet/test-video.mp4`

## 优势

1. **简化部署**: 不再需要额外的图片服务器
2. **统一管理**: 所有服务都在一个Node.js进程中
3. **更好的控制**: 可以添加自定义的访问控制、日志记录等功能
4. **减少依赖**: 减少了外部服务的依赖
5. **性能优化**: 减少了网络跳转，提高文件访问速度 